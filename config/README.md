# config/

Shared platform configuration roots that are **not** environment-scoped.

| Path | Role |
|---|---|
| [`vwan/`](vwan/) | Shared Azure Virtual WAN — apply before env connectivity hubs |

Env-scoped stacks live under [`../environments`](../environments). Reusable logic lives under [`../modules`](../modules).
