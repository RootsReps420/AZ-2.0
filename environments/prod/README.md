# MOVED — use `environments/prd`

This `environments/prod` folder is obsolete. Production stacks live under [`../prd`](../prd) (TDA environment code `prd`).

Delete this directory when nothing holds locks on `.terraform` provider binaries (IDE / terraform processes).
