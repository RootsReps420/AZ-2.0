# terraform.tfvars.example — prd/connectivity

Deploy values for **uksouth** live in:

[`../../region/uksouth/prd.connectivity.tfvars`](../../region/uksouth/prd.connectivity.tfvars)

```bash
terraform plan \
  -var-file=../../region/uksouth/prd.connectivity.tfvars \
  -var=location=uksouth
```
