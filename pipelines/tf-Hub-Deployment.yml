# Connectivity hubs (Hub01 secured / Hub02 unsecured + VPN). One connectivity state file.
# Requires _global (vWAN) first. hubSelection:
#   hub01 — firewall policy + Hub01 only
#   hub02 — add/keep Hub02 + VPN (keeps Hub01 enabled)
#   both  — Hub01 + Hub02 in one apply (phased first-time: run hub01 then hub02)

trigger: none

parameters:
  - name: envName
    displayName: Environment
    type: string
    default: int
    values:
      - int
      - prd
  - name: hubSelection
    displayName: Hubs to manage
    type: string
    default: both
    values:
      - both
      - hub01
      - hub02
  - name: action
    displayName: Action
    type: string
    default: plan
    values:
      - plan
      - apply
      - destroy

variables:
  - name: azureServiceConnection
    ${{ if eq(parameters.envName, 'int') }}:
      value: SC-R-VDI-INT-C-01
    ${{ if eq(parameters.envName, 'prd') }}:
      value: SC-P-VDI-PRD-C-01
  - name: agentPool
    ${{ if eq(parameters.envName, 'int') }}:
      value: uks-int-vdi-mgmt-vss-01
    ${{ if eq(parameters.envName, 'prd') }}:
      value: uks-prd-vdi-mgmt-vss-01
  - name: tfBackendResourceGroup
    value: $(tf.backend.resourceGroup)
  - name: tfBackendStorageAccount
    value: $(tf.backend.storageAccount)
  - name: tfBackendContainer
    value: tfstate

stages:
  - template: templates/connectivity-stages.yml
    parameters:
      envName: ${{ parameters.envName }}
      azureServiceConnection: $(azureServiceConnection)
      agentPool: $(agentPool)
      tfBackendResourceGroup: $(tfBackendResourceGroup)
      tfBackendStorageAccount: $(tfBackendStorageAccount)
      tfBackendContainer: $(tfBackendContainer)
      action: ${{ parameters.action }}
      hubSelection: ${{ parameters.hubSelection }}
      workingDirectory: environments/${{ parameters.envName }}/connectivity
